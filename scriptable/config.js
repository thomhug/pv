// In diesem Script befinden sich die Credentials für deinen SolarManager sowie deine Solarmanager ID.
// Die Solarmanger ID (smid) findest du in der App (Profil)
// Das Auth Token erhältst du, wenn du dich hier: https://external-web.solar-manager.ch/swagger einloggst, dann 'try it out' und 'execute'. Etwas weiter unten nach 'authorization: Basic ...' suchen und copy pasten.
module.exports = {
  auth: "Basic xxx",
  smid: "xxx"
}
